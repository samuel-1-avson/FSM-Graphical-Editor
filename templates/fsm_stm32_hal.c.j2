/*
 * STM32 HAL FSM for: {{ fsm_name_c }}
 * Generated by: BSM Designer
 *
 * How to integrate:
 * 1. Add this .c file and the corresponding .h file to your STM32CubeIDE project.
 * 2. In main.c, include "{{ fsm_name_c }}.h" inside the `/* USER CODE BEGIN Includes */` section.
 * 3. In main.c, call `{{ fsm_name_c }}_init();` inside the `/* USER CODE BEGIN 2 */` section after your hardware inits.
 * 4. In main.c, inside the `while (1)` loop's `/* USER CODE BEGIN 3 */` section, add your event detection
 *    logic and call `{{ fsm_name_c }}_run(event);`.
 */
#include "{{ fsm_name_c }}.h"

// --- FSM IMPLEMENTATION (Auto-Generated) ---
// (This section is the same as the Arduino template's FSM logic)
// ...

// --- ACTION IMPLEMENTATIONS (User Stubs) ---
// TODO: Implement your action logic here using STM32 HAL functions.
// Example stubs will use functions like HAL_GPIO_WritePin().
// Make sure you have configured these GPIOs in CubeMX.
// Example:
// void on_entry_StateOn(void) {
//   HAL_GPIO_WritePin(LD2_GPIO_Port, LD2_Pin, GPIO_PIN_SET);
// }
// ...